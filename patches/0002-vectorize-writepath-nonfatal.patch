diff --git a/src/capture/duplicate.ts b/src/capture/duplicate.ts
index ef2f1ef..914dc6e 100644
--- a/src/capture/duplicate.ts
+++ b/src/capture/duplicate.ts
@@ -44,7 +44,12 @@ export async function checkDuplicateAndContradiction(content: string, env: Env):
 }> {
   const sample = getDuplicateCheckSample(content);
   const values = await embed(sample, env);
-  const { matches } = await env.VECTORIZE.query(values, { topK: 5, returnMetadata: "all" });
+  let matches: VectorizeMatch[] = [];
+  try {
+    ({ matches } = await env.VECTORIZE.query(values, { topK: 5, returnMetadata: "all" }));
+  } catch (e) {
+    console.error("Vectorize duplicate-check query failed (non-fatal, skipping dedupe):", e);
+  }
 
   const neighborScores = new Map<string, number>();
   for (const m of matches) {
diff --git a/src/capture/store.ts b/src/capture/store.ts
index 878abb2..c7118f1 100644
--- a/src/capture/store.ts
+++ b/src/capture/store.ts
@@ -39,7 +39,11 @@ export async function storeEntry(
     })
   );
 
-  await env.VECTORIZE.upsert(vectors);
+  try {
+    await env.VECTORIZE.upsert(vectors);
+  } catch (e) {
+    console.error("Vectorize upsert failed (non-fatal, keyword-only mode):", e);
+  }
 
   const vectorIds = vectors.map(v => v.id);
 
@@ -53,7 +57,11 @@ export async function storeEntry(
 export async function deleteStaleVectors(env: Env, oldIds: string[], newIds: string[]): Promise<void> {
   if (!newIds.length) return;
   const stale = oldIds.filter(v => !newIds.includes(v));
-  if (stale.length) await env.VECTORIZE.deleteByIds(stale);
+  try {
+    if (stale.length) await env.VECTORIZE.deleteByIds(stale);
+  } catch (e) {
+    console.error("Vectorize deleteByIds failed (non-fatal, keyword-only mode):", e);
+  }
 }
 
 export async function reembedOrThrow(env: Env, id: string, content: string, tags: string[], source: string): Promise<string[]> {
@@ -118,11 +126,15 @@ export async function appendToEntry(
     metadata[`tag_${t.replace(/[."]/g, "_")}`] = true;
   });
 
-  await env.VECTORIZE.insert([{
-    id: newChunkId,
-    values,
-    metadata,
-  }]);
+  try {
+    await env.VECTORIZE.insert([{
+      id: newChunkId,
+      values,
+      metadata,
+    }]);
+  } catch (e) {
+    console.error("Vectorize insert failed (non-fatal, keyword-only mode):", e);
+  }
 
   await env.DB.prepare(
     `UPDATE entries SET content = ?, vector_ids = ? WHERE id = ?`
diff --git a/src/graph/traverse.ts b/src/graph/traverse.ts
index 0bc5417..caea0f9 100644
--- a/src/graph/traverse.ts
+++ b/src/graph/traverse.ts
@@ -201,7 +201,12 @@ export async function buildGraph(opts: { seed?: string; limit?: number }, env: E
 }
 
 export async function neighborsFromVectorQuery(values: number[], env: Env): Promise<{ id: string; score: number }[]> {
-  const { matches } = await env.VECTORIZE.query(values, { topK: 5, returnMetadata: "all" });
+  let matches: VectorizeMatch[] = [];
+  try {
+    ({ matches } = await env.VECTORIZE.query(values, { topK: 5, returnMetadata: "all" }));
+  } catch (e) {
+    console.error("Vectorize neighbor query failed (non-fatal, no graph neighbors):", e);
+  }
   const scores = new Map<string, number>();
   for (const m of matches) {
     const pid = (m.metadata as any)?.parentId ?? m.id;
